Merges data from excel files into a target sqlite database

1. Make sure that temoa_schema.sqlite is in this folder
2. Add all excel files to be merged into this folder (sub-folders are fine)
3. Run merge_excel_into_target

Any additional columns not existing in the schema will be ignored.